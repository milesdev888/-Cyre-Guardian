/* vortex.js — retired (was Solana multi-color mesh = Xmas-tree look).
   Hero background now lives in /hero-meteors.js (sapphire glass only).
   Kept as a no-op so stale cached HTML that still loads vortex.js draws nothing. */
(function () { 'use strict'; })();
